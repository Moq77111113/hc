// Package main is probegen: it reads the probe catalog and regenerates every
// packaging artifact derived from it — .goreleaser.yaml, the CI slim matrix,
// and the README bundle table.
package main
