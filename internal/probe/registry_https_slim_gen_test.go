// Code generated by probegen; DO NOT EDIT.
//go:build hc_slim && hc_https && !hc_http && !hc_tcp && !hc_postgres && !hc_mysql && !hc_redis

package probe

import "testing"

func TestSlimBuild_https(t *testing.T) {
	present := []string{"https"}
	absent := []string{"http", "tcp", "postgres", "pg", "mysql", "redis"}
	for _, s := range present {
		if _, ok := probers[s]; !ok {
			t.Errorf("scheme %q must be registered in slim https build", s)
		}
	}
	for _, s := range absent {
		if _, ok := probers[s]; ok {
			t.Errorf("scheme %q must NOT be registered in slim https build", s)
		}
	}
}
